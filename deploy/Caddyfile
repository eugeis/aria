# Reverse proxy in front of eugeis: automatic Let's Encrypt TLS, HSTS.
#   caddy adapt  -> Caddyfile check
#   caddy run --config /etc/caddy/Caddyfile

eugeis.example.com {
    encode gzip zstd
    reverse_proxy 127.0.0.1:8080
}
